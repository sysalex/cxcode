# 编码规范

本文档定义基础编码要求。前端框架、后端框架和数据库确定后，需要补充对应技术栈的具体规则。

## 通用规则

- 优先使用清晰的领域命名，不使用含糊的通用名称。
- 函数应足够小，便于直接测试。
- 避免隐藏的全局状态。
- API 输入、输出、事件、错误必须有明确类型或 schema。
- 时间、身份、随机数、外部服务必须可注入，便于测试。
- 边界层负责输入校验，领域层负责业务不变量。
- 不允许把基础设施异常直接暴露给 API 调用方。
- 不允许记录密码、token、密钥、答案明文、答案 key 等敏感信息。

## 错误处理

异常体系见 `docs/errors.md`。

所有对用户可见的错误必须包含：

- 稳定错误码
- 安全错误信息
- HTTP 状态或传输层状态
- requestId
- 可选结构化 details

## 日志

只使用结构化日志。日志事件名和关联字段必须遵守 `docs/observability.md`。

## 注释

只为不明显的业务规则、不变量、设计权衡写注释。不要给机械赋值或显而易见的框架代码写注释。

## 测试

- 新增领域行为必须有领域测试。
- 新增 API 行为必须有契约测试或集成测试。
- 涉及安全和权限的行为必须有反向用例测试。
